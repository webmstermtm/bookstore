// In-memory users (no DB)
// Store: { username, passwordHash }
const users = [
  // sample pre-registered user for quick testing (password: "password")
  // You may replace this with your own user via /auth/register
];

module.exports = { users };
